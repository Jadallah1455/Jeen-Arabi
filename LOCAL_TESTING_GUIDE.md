# 🚀 دليل تشغيل التطبيق محلياً مع قاعدة البيانات على الويب

## الخطوات البسيطة

### 1️⃣ إنشاء ملف `.env.local` في المجلد الرئيسي

أنشئ ملف جديد اسمه `.env.local` في `jeen-arabi/` (نفس مستوى `package.json`):

```env
# الاتصال بقاعدة البيانات على الويب
VITE_API_BASE_URL=https://kids.genarabi.com/api
```

**ملاحظة:** هذا الملف سيجعل التطبيق المحلي يتصل بالسيرفر على الويب بدلاً من localhost.

---

### 2️⃣ تشغيل التطبيق

افتح Terminal في مجلد `jeen-arabi` واكتب:

```bash
npm run dev
```

سيفتح التطبيق على: `http://localhost:5173`

---

### 3️⃣ التأكد من إعدادات CORS على السيرفر

يجب أن يكون السيرفر يدعم CORS من localhost. تحقق من الملف `server/server.js`:

```javascript
// CORS Configuration
const corsOptions = {
    origin: [
        'https://kids.genarabi.com',
        'http://localhost:5173',  // ← مهم للتطوير المحلي
        'http://localhost:3000'
    ],
    credentials: true
};
app.use(cors(corsOptions));
```

**إذا لم يكن موجود:** أضف `http://localhost:5173` للـ CORS origins على السيرفر.

---

## 🎯 ماذا سيحدث؟

✅ **Frontend:** يعمل محلياً على `http://localhost:5173`  
✅ **Backend:** متصل بـ `https://kids.genarabi.com/api`  
✅ **Database:** قاعدة البيانات الحقيقية على الويب  
✅ **Data:** ستظهر كل القصص، المستخدمين، الآراء من قاعدة البيانات الموجودة

---

## 📌 الفوائد

🔍 **اختبار كامل:** تجرب التطبيق قبل الرفع  
🌐 **بيانات حقيقية:** تشوف القصص والمستخدمين الفعليين  
⚡ **تطوير سريع:** أي تعديل في الكود يظهر فوراً  
🔒 **آمن:** لا تعديل على البيانات الحقيقية إلا بالتسجيل

---

## ⚠️ ملاحظات مهمة

1. **CORS:** تأكد أن السيرفر يسمح بـ localhost:5173
2. **Cookies:** قد تحتاج تسجيل دخول جديد (الـ cookies محلية)
3. **Images:** الصور ستُحمّل من الويب (Cloudinary)
4. **Speed:** قد يكون أبطأ قليلاً لأن البيانات من الويب

---

## 🛠️ حل المشاكل الشائعة

### مشكلة CORS
**الخطأ:** `Access to XMLHttpRequest has been blocked by CORS policy`

**الحل:** أضف localhost للـ CORS في `server/server.js`:
```javascript
origin: [
    'https://kids.genarabi.com',
    'http://localhost:5173',
    'http://localhost:3000'
],
```

### مشكلة الاتصال
**الخطأ:** `Network Error` أو `ERR_CONNECTION_REFUSED`

**الحل:** 
- تأكد أن السيرفر على الويب يعمل
- تأكد من رابط API صحيح في `.env.local`

### مشكلة الـ Cookies
**الخطأ:** `Authentication failed`

**الحل:**
- سجل دخول من جديد
- تأكد من `withCredentials: true` في `api.ts` (موجود ✓)

---

## 🎉 الأمر النهائي

```bash
# في مجلد jeen-arabi
npm run dev
```

ثم افتح المتصفح على: **http://localhost:5173**

سترى التطبيق المحلي مع كل البيانات من قاعدة البيانات على الويب! 🚀
